From 863473098383fe88e9e536b99803c94dd72d3bf9 Mon Sep 17 00:00:00 2001
From: tanghaiyun <19855679967@163.com>
Date: Tue, 22 Jul 2025 11:58:54 +0800
Subject: [PATCH] support tx udp tcp csum hw offload

---
 drivers/net/hinic3/base/hinic3_compat.h       | 10 +--
 drivers/net/hinic3/base/hinic3_pmd_hwdev.c    |  4 -
 drivers/net/hinic3/base/hinic3_pmd_nic_cfg.h  |  2 +-
 .../net/hinic3/base/hinic3_pmd_nic_event.c    |  5 --
 drivers/net/hinic3/hinic3_pmd_bifur.c         | 46 +++-------
 drivers/net/hinic3/hinic3_pmd_ethdev.c        | 48 ++--------
 drivers/net/hinic3/hinic3_pmd_flow.c          | 32 ++++++-
 drivers/net/hinic3/hinic3_pmd_tx.c            | 90 +++++++++++++++++++
 drivers/net/hinic3/meson.build                | 12 +--
 9 files changed, 148 insertions(+), 101 deletions(-)

diff --git a/drivers/net/hinic3/base/hinic3_compat.h b/drivers/net/hinic3/base/hinic3_compat.h
index 0a98a60..5483a51 100644
--- a/drivers/net/hinic3/base/hinic3_compat.h
+++ b/drivers/net/hinic3/base/hinic3_compat.h
@@ -14,13 +14,13 @@
 #include <rte_common.h>
 #include <rte_byteorder.h>
 #include <rte_memzone.h>
-#ifdef DPDK_21_11
-#include <ethdev_pci.h>
-#include <eal_interrupts.h>
-#else
+// #ifdef DPDK_21_11
+// #include <ethdev_pci.h>
+// #include <eal_interrupts.h>
+// #else
 #include <rte_ethdev_driver.h>
 #include <rte_ethdev_pci.h>
-#endif
+// #endif
 #include <rte_memcpy.h>
 #include <rte_malloc.h>
 #include <rte_atomic.h>
diff --git a/drivers/net/hinic3/base/hinic3_pmd_hwdev.c b/drivers/net/hinic3/base/hinic3_pmd_hwdev.c
index 0ec5f90..3073fb7 100644
--- a/drivers/net/hinic3/base/hinic3_pmd_hwdev.c
+++ b/drivers/net/hinic3/base/hinic3_pmd_hwdev.c
@@ -81,10 +81,6 @@ struct mgmt_event_handle {
 	mgmt_event_cb proc;
 };
 
-#ifdef DPDK_20_11
-#define RTE_KDRV_VFIO RTE_PCI_KDRV_VFIO
-#endif
-
 bool hinic3_is_vfio_iommu_enable(const struct rte_eth_dev *rte_dev)
 {
 	return ((RTE_ETH_DEV_TO_PCI(rte_dev)->kdrv == RTE_KDRV_VFIO) && /*lint !e507*/
diff --git a/drivers/net/hinic3/base/hinic3_pmd_nic_cfg.h b/drivers/net/hinic3/base/hinic3_pmd_nic_cfg.h
index c5c5c19..a17855d 100644
--- a/drivers/net/hinic3/base/hinic3_pmd_nic_cfg.h
+++ b/drivers/net/hinic3/base/hinic3_pmd_nic_cfg.h
@@ -884,7 +884,7 @@ struct hinic3_port_flow_bifur_en_cmd {
 	u8 flow_bifur_type; /* 0: vf bifur, 2: traffic bifur */
 	u8 config_flag;
 	u8 iso_en; /* 0 -> off, 1 -> on */
-	u8 rsvd[3];
+	u8 rsvd[2];
 };
 
 struct hinic3_fdir_del_rule {
diff --git a/drivers/net/hinic3/base/hinic3_pmd_nic_event.c b/drivers/net/hinic3/base/hinic3_pmd_nic_event.c
index 4622710..f414427 100644
--- a/drivers/net/hinic3/base/hinic3_pmd_nic_event.c
+++ b/drivers/net/hinic3/base/hinic3_pmd_nic_event.c
@@ -112,13 +112,8 @@ static void link_status_event_handler(void *hwdev, void *buf_in,
 	err = rte_eth_linkstatus_set((struct rte_eth_dev *)(dev->eth_dev),
 				     &link);
 	if (!err)
-#ifndef DPDK_20_11
 		_rte_eth_dev_callback_process(dev->eth_dev,
 					      RTE_ETH_EVENT_INTR_LSC, NULL);
-#else
-		rte_eth_dev_callback_process(dev->eth_dev,
-					      RTE_ETH_EVENT_INTR_LSC, NULL);
-#endif
 }
 
 static const char *get_port_type_name(u32 type)
diff --git a/drivers/net/hinic3/hinic3_pmd_bifur.c b/drivers/net/hinic3/hinic3_pmd_bifur.c
index 38059fd..8b9eaca 100644
--- a/drivers/net/hinic3/hinic3_pmd_bifur.c
+++ b/drivers/net/hinic3/hinic3_pmd_bifur.c
@@ -12,13 +12,10 @@
 #include <fcntl.h>
 #include <errno.h>
 
-#if defined(DPDK_21_BIFUR) || defined(DPDK_20_BIFUR)
 #include <rte_bus.h>
-#else
-#include <bus_driver.h>
-#endif
+#include <rte_string_fns.h>
 
-#ifndef DPDK_21_11
+#ifdef DPDK_19_11
 #define RTE_PCI_ANY_ID (0xffff)
 #define RTE_INTR_INSTANCE_F_PRIVATE UINT32_C(0)
 #endif
@@ -186,15 +183,6 @@ static int hinic3_bifur_pci_map_device(struct rte_pci_device *dev)
 {
     int ret = 0;
     ret = rte_pci_map_device(dev);
-#ifdef DPDK_21_BIFUR
-    if (ret != 0) {
-        rte_intr_instance_free(dev->vfio_req_intr_handle);
-        dev->vfio_req_intr_handle = NULL;
-        rte_intr_instance_free(dev->intr_handle);
-        dev->intr_handle = NULL;
-    }
-#endif
-
     return ret;
 }
 
@@ -357,7 +345,7 @@ static enum rte_iova_mode hinic3_bifur_pci_device_iova_mode(const struct rte_pci
     enum rte_iova_mode iova_mode = RTE_IOVA_DC;
 
     switch (pdev->kdrv) {
-    case RTE_PCI_KDRV_VFIO: {
+    case RTE_KDRV_VFIO: {
     #ifdef VFIO_PRESENT
         static int is_vfio_noiommu_enabled = -1;
 
@@ -378,8 +366,8 @@ static enum rte_iova_mode hinic3_bifur_pci_device_iova_mode(const struct rte_pci
     #endif
         break;
     }
-    case RTE_PCI_KDRV_IGB_UIO:
-    case RTE_PCI_KDRV_UIO_GENERIC:
+    case RTE_KDRV_IGB_UIO:
+    case RTE_KDRV_UIO_GENERIC:
         iova_mode = RTE_IOVA_PA;
         break;
     default:
@@ -617,13 +605,13 @@ static struct rte_pci_device *hinic3_bifur_alloc_pci_dev(const char *dirname,
 
     if (!ret) {
         if (!strcmp(driver, "vfio-pci")) {
-            dev->kdrv = RTE_PCI_KDRV_VFIO;
+            dev->kdrv = RTE_KDRV_VFIO;
         } else if (!strcmp(driver, "igb_uio")) {
-            dev->kdrv = RTE_PCI_KDRV_IGB_UIO;
+            dev->kdrv = RTE_KDRV_IGB_UIO;
         } else if (!strcmp(driver, "uio_pci_generic")) {
-            dev->kdrv = RTE_PCI_KDRV_UIO_GENERIC;
+            dev->kdrv = RTE_KDRV_UIO_GENERIC;
         } else {
-            dev->kdrv = RTE_PCI_KDRV_UNKNOWN;
+            dev->kdrv = RTE_KDRV_UNKNOWN;
         }
     } else {
         free(dev);
@@ -647,7 +635,7 @@ static int hinic3_bifur_work_pci_pre_probe(struct rte_pci_driver *dr, struct rte
     }
 
     if (dev->device.devargs != NULL &&
-        dev->device.devargs->policy == RTE_DEV_BLOCKED) {
+        dev->device.devargs->policy == RTE_DEV_BLACKLISTED) {
         PMD_DRV_LOG(INFO, "Device is blocked, not initializing");
         return 1;
     }
@@ -679,21 +667,7 @@ static int hinic3_bifur_work_pci_pre_probe(struct rte_pci_driver *dr, struct rte
                 iova_mode == RTE_IOVA_PA ? "PA": "VA");
             return -EINVAL;
         }
-#ifdef DPDK_21_BIFUR
-        dev->intr_handle = rte_intr_instance_alloc(RTE_INTR_INSTANCE_F_PRIVATE);
-        if (dev->intr_handle == NULL) {
-            PMD_DRV_LOG(ERR, "Failed to create interrupt instance for %s", dev->device.name);
-            return -ENOMEM;
-        }
 
-        dev->vfio_req_intr_handle = rte_intr_instance_alloc(RTE_INTR_INSTANCE_F_PRIVATE);
-        if (dev->vfio_req_intr_handle == NULL) {
-            rte_intr_instance_free(dev->intr_handle);
-            dev->intr_handle = NULL;
-            PMD_DRV_LOG(ERR, "Failed to create vfio req interrupt instance for %s", dev->device.name);
-            return -ENOMEM;
-        }
-#endif
         dev->driver = dr;
     }
 
diff --git a/drivers/net/hinic3/hinic3_pmd_ethdev.c b/drivers/net/hinic3/hinic3_pmd_ethdev.c
index 4c1e945..95bcd32 100644
--- a/drivers/net/hinic3/hinic3_pmd_ethdev.c
+++ b/drivers/net/hinic3/hinic3_pmd_ethdev.c
@@ -9,6 +9,7 @@
 #include <rte_mempool.h>
 #include <rte_errno.h>
 #include <rte_ether.h>
+#include <rte_ethdev.h>
 
 #include "base/hinic3_compat.h"
 #include "base/hinic3_pmd_csr.h"
@@ -1750,11 +1751,9 @@ static void hinic3_copy_mempool_uninit(struct hinic3_nic_dev *nic_dev)
  * @param[in] dev
  *   Pointer to ethernet device structure.
  */
-#ifdef DPDK_20_11
-static int hinic3_dev_stop(struct rte_eth_dev *dev)
-#else
+
 static void hinic3_dev_stop(struct rte_eth_dev *dev)
-#endif
+
 {
 	struct hinic3_nic_dev *nic_dev;
 	struct rte_eth_link link;
@@ -1765,9 +1764,6 @@ static void hinic3_dev_stop(struct rte_eth_dev *dev)
 				       &nic_dev->dev_status)) {
 		PMD_DRV_LOG(INFO, "Device %s already stopped",
 			    nic_dev->dev_name);
-#ifdef DPDK_20_11
-		return 0;
-#endif
 	}
 
 	/* Stop phy port and vport */
@@ -1813,10 +1809,6 @@ static void hinic3_dev_stop(struct rte_eth_dev *dev)
 
 	/* Free mempool */
 	hinic3_copy_mempool_uninit(nic_dev);
-
-#ifdef DPDK_20_11
-	return 0;
-#endif
 }
 
 static void hinic3_dev_release(struct rte_eth_dev *eth_dev)
@@ -1875,36 +1867,23 @@ static void hinic3_dev_release(struct rte_eth_dev *eth_dev)
  * @param[in] dev
  *   Pointer to ethernet device structure.
  */
-#ifdef DPDK_20_11
-static int hinic3_dev_close(struct rte_eth_dev *eth_dev)
-#else
+
 static void hinic3_dev_close(struct rte_eth_dev *eth_dev)
-#endif
+
 {
 	struct hinic3_nic_dev *nic_dev =
 		HINIC3_ETH_DEV_TO_PRIVATE_NIC_DEV(eth_dev);
-#ifdef DPDK_20_11
-	int ret;
-#endif
+
 
 	if (hinic3_test_and_set_bit(HINIC3_DEV_CLOSE, &nic_dev->dev_status)) {
 		PMD_DRV_LOG(WARNING, "Device %s already closed",
 			    nic_dev->dev_name);
-#ifdef DPDK_20_11
-		return 0;
-#endif
 	}
 
-#ifdef DPDK_20_11
-	ret = hinic3_dev_stop(eth_dev);
-#else
-	hinic3_dev_stop(eth_dev);
-#endif
 
+	hinic3_dev_stop(eth_dev);
 	hinic3_dev_release(eth_dev);
-#ifdef DPDK_20_11
-	return ret;
-#endif
+
 }
 
 static int hinic3_dev_reset(__rte_unused struct rte_eth_dev *dev)
@@ -3532,13 +3511,6 @@ static int hinic3_func_init(struct rte_eth_dev *eth_dev)
 	PMD_DRV_LOG(INFO, "Initialize %s in primary succeed",
 		    eth_dev->data->name);
 
-#ifdef DPDK_20_11
-	/**
-	 * Queue xstats filled automatically by ethdev layer.
-	 */
-	eth_dev->data->dev_flags |= RTE_ETH_DEV_AUTOFILL_QUEUE_XSTATS;
-#endif
-
 	return 0;
 
 enable_intr_fail:
@@ -3609,13 +3581,9 @@ static int hinic3_dev_uninit(struct rte_eth_dev *dev)
 	if (rte_eal_process_type() != RTE_PROC_PRIMARY)
 		return 0;
 
-#ifdef DPDK_20_11
-	return hinic3_dev_close(dev);
-#else
 	hinic3_dev_close(dev);
 
 	return 0;
-#endif
 }
 
 static const struct rte_pci_id pci_id_hinic3_map[] = {
diff --git a/drivers/net/hinic3/hinic3_pmd_flow.c b/drivers/net/hinic3/hinic3_pmd_flow.c
index eb617fd..20e60c2 100644
--- a/drivers/net/hinic3/hinic3_pmd_flow.c
+++ b/drivers/net/hinic3/hinic3_pmd_flow.c
@@ -25,13 +25,14 @@
 #include "hinic3_pmd_flow.h"
 
 #ifdef HINIC3_TRAFFIC_BIFUR
-#include <rte_bitops.h>
 #include "hinic3_pmd_rx.h"
 #include "base/hinic3_pmd_csr.h"
 #include "hinic3_pmd_bifur.h"
 
 #define HINIC3_QUEUE_MAX 16
 #define HINIC3_QUEUE_ALLOW_NUM 8
+
+#define RTE_BIT32(nr) (UINT32_C(1) << (nr))
 #endif
 
 #define HINIC3_UINT8_MAX          0xff
@@ -323,6 +324,35 @@ static const struct hinic3_valid_pattern hinic3_supported_patterns[] = {
 
 };
 
+#ifdef HINIC3_TRAFFIC_BIFUR
+static inline uint32_t
+rte_bit_relaxed_get32(unsigned int nr, volatile uint32_t *addr)
+{
+	RTE_ASSERT(nr < 32);
+
+	uint32_t mask = UINT32_C(1) << nr;
+	return (*addr) & mask;
+}
+
+static inline void
+rte_bit_relaxed_set32(unsigned int nr, volatile uint32_t *addr)
+{
+	RTE_ASSERT(nr < 32);
+
+	uint32_t mask = RTE_BIT32(nr);
+	*addr = (*addr) | mask;
+}
+
+static inline void
+rte_bit_relaxed_clear32(unsigned int nr, volatile uint32_t *addr)
+{
+	RTE_ASSERT(nr < 32);
+
+	uint32_t mask = RTE_BIT32(nr);
+	*addr = (*addr) & (~mask);
+}
+#endif
+
 static inline void net_addr_to_host(uint32_t *dst, const uint32_t *src, size_t len)
 {
 	size_t i;
diff --git a/drivers/net/hinic3/hinic3_pmd_tx.c b/drivers/net/hinic3/hinic3_pmd_tx.c
index 8cb622c..b03d297 100644
--- a/drivers/net/hinic3/hinic3_pmd_tx.c
+++ b/drivers/net/hinic3/hinic3_pmd_tx.c
@@ -325,6 +325,93 @@ int hinic3_tx_done_cleanup(void *txq, u32 free_cnt)
 	return hinic3_xmit_mbuf_cleanup(tx_queue, try_free_cnt);
 }
 
+static inline void hinic3_calculate_tcp_checksum(struct rte_mbuf *mbuf,
+					u16 inner_l3_offset)
+{
+	struct rte_ipv4_hdr *ipv4_hdr;
+	struct rte_ipv6_hdr *ipv6_hdr;
+	struct rte_tcp_hdr *tcp_hdr;
+	uint64_t ol_flags = mbuf->ol_flags;
+
+	if (ol_flags & HINIC3_PKT_TX_IPV4) {
+		ipv4_hdr = rte_pktmbuf_mtod_offset(mbuf, struct rte_ipv4_hdr *,
+							inner_l3_offset);
+
+		if (ol_flags & HINIC3_PKT_TX_IP_CKSUM)
+			ipv4_hdr->hdr_checksum = 0;
+
+		tcp_hdr = (struct rte_tcp_hdr *)((char *)ipv4_hdr +
+						mbuf->l3_len);
+		tcp_hdr->cksum = rte_ipv4_phdr_cksum(ipv4_hdr, ol_flags);
+	} else {
+		ipv6_hdr = rte_pktmbuf_mtod_offset(mbuf, struct rte_ipv6_hdr *,
+							inner_l3_offset);
+		tcp_hdr = rte_pktmbuf_mtod_offset(mbuf, struct rte_tcp_hdr *,
+							(inner_l3_offset +
+							mbuf->l3_len));
+		tcp_hdr->cksum = rte_ipv6_phdr_cksum(ipv6_hdr, ol_flags);
+	}
+
+	return;
+}
+
+static inline void hinic3_calculate_udp_checksum(struct rte_mbuf *mbuf,
+					u16 inner_l3_offset)
+{
+	struct rte_ipv4_hdr *ipv4_hdr;
+	struct rte_ipv6_hdr *ipv6_hdr;
+	struct rte_udp_hdr *udp_hdr;
+	uint64_t ol_flags = mbuf->ol_flags;
+
+	if (ol_flags & HINIC3_PKT_TX_IPV4) {
+		ipv4_hdr = rte_pktmbuf_mtod_offset(mbuf, struct rte_ipv4_hdr *,
+							inner_l3_offset);
+
+		if (ol_flags & HINIC3_PKT_TX_IP_CKSUM)
+			ipv4_hdr->hdr_checksum = 0;
+
+		udp_hdr = (struct rte_udp_hdr *)((char *)ipv4_hdr +
+						mbuf->l3_len);
+		udp_hdr->dgram_cksum = rte_ipv4_phdr_cksum(ipv4_hdr, ol_flags);
+	} else {
+		ipv6_hdr = rte_pktmbuf_mtod_offset(mbuf, struct rte_ipv6_hdr *,
+							inner_l3_offset);
+		udp_hdr = rte_pktmbuf_mtod_offset(mbuf, struct rte_udp_hdr *,
+							(inner_l3_offset +
+							mbuf->l3_len));
+		udp_hdr->dgram_cksum = rte_ipv6_phdr_cksum(ipv6_hdr, ol_flags);
+	}
+
+	return;
+}
+
+static inline void hinic3_calculate_checksum(struct rte_mbuf *mbuf,
+					u16 inner_l3_offset)
+{
+	uint64_t ol_flags = mbuf->ol_flags;
+
+	switch (ol_flags & HINIC3_PKT_TX_L4_MASK) {
+		case HINIC3_PKT_TX_UDP_CKSUM:
+			hinic3_calculate_udp_checksum(mbuf, inner_l3_offset);
+			break;
+
+		case HINIC3_PKT_TX_TCP_CKSUM:
+			hinic3_calculate_tcp_checksum(mbuf, inner_l3_offset);
+			break;
+
+		case HINIC3_PKT_TX_SCTP_CKSUM:
+			/* Sctp csum no need to calculate pseudo-header */
+			break;
+
+		default:
+			if (ol_flags & HINIC3_PKT_TX_TCP_SEG)
+				hinic3_calculate_tcp_checksum(mbuf, inner_l3_offset);
+			break;
+	}
+
+	return;
+}
+
 /*lint -e40*/
 static int hinic3_tx_offload_pkt_prepare(struct rte_mbuf *mbuf,
 					 u16 *inner_l3_offset)
@@ -362,6 +449,9 @@ static int hinic3_tx_offload_pkt_prepare(struct rte_mbuf *mbuf,
 		*inner_l3_offset = mbuf->l2_len;
 	}
 
+	/* Process the pseudo-header checksum */
+	hinic3_calculate_checksum(mbuf, *inner_l3_offset);
+
 	return 0;
 }
 /*lint +e40*/
diff --git a/drivers/net/hinic3/meson.build b/drivers/net/hinic3/meson.build
index daddef7..1e9ddd3 100644
--- a/drivers/net/hinic3/meson.build
+++ b/drivers/net/hinic3/meson.build
@@ -19,15 +19,9 @@ cflags += ['-DHINIC3_TRAFFIC_BIFUR']
 cflags += ['-fPIC']
 cflags += ['-fstack-protector-strong']
 
-dpdk_version = meson.project_version()
-if dpdk_version.substring(0,2) == '20'
-	cflags += ['-DDPDK_20_11']
-	cflags += ['-DDPDK_20_BIFUR']
-elif dpdk_version.substring(0,2) == '21'
-	cflags += ['-DDPDK_20_11']
-	cflags += ['-DDPDK_21_11']
-	cflags += ['-DDPDK_21_BIFUR']
-endif
+cflags += ['-DDPDK_19_11']
+cflags += ['-DDPDK_20_11']
+cflags += ['-DDPDK_19_BIFUR']
 
 subdir('base')
 subdir('mml')
-- 
2.45.1.windows.1

